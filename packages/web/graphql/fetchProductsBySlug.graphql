query fetchProductsBySlug($slug: String!) {
  getProductBySlug(slug: $slug) {
    id
    sku
    name
    slug
    imageUrl
    imageKey
    description
    quantity
    price
    taxable
    isActive
  }
}
